{
  "name": "server",
  "version": "1.0.0",
  "description": "This is a server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "NVM",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^2.0.3"
  }
}
